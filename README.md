# ping_monitor

一个基于 Tkinter 的本地网络链路监控工具，可监控外网目标和本地网关的连通性，通过可视化界面显示实时延迟、历史趋势和故障判定。

## 功能

- 监控外网目标（默认 `223.5.5.5`）
- 监控本地网关（自动识别默认网关）
- 记录断线事件和持续时间
- 实时诊断本地/外部故障原因
- 生成图表和表格视图

## 运行

```bash
python ping_monitor.py
```

## 忽略文件

- `ping_history.db`（本地数据库）
- Python 缓存文件
- 虚拟环境目录
